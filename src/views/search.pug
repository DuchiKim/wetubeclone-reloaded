extends base.pug
include mixins/video

block content
    form(method="GET") 
        input(type="text" , placeholder="search videos" , name="keyword")
        input(type="submit" , value="Search")

    div
        each video in videos
            +video(video)